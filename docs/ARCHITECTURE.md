# Implementation notes

- **LidSensor.swift:** read-only IOKit HID access, Apple vendor/product `05ac:8104`, usage `0020:008a`, feature report 1. Whole-degree values are read on a serial worker at 30 Hz, with bounded reconnect backoff. Protocol support is undocumented.
- **ClosingMotion.swift:** gesture and threshold state, including awake reversal and startup/reset behavior.
- **DesktopCapture.swift:** one native-resolution sRGB ScreenCaptureKit screenshot per gesture, excluding Glissform itself. No stream is started. Pixels remain in memory.
- **EffectRenderer.swift:** runtime-compiled Metal projection and frost/shadow, with a fixed viewpoint approximation. Camera distance is four screen heights, eye height 1.1 screen heights, and rotation gain 0.867. The gradient runs from the bottom to a top 5% black plateau at full displayed progress.
- **MotionSmoothing.swift:** two-stage adaptive filtering for quantized angles and 180 ms boundary handoffs. Large movements/reversals use a fast response; slow steps deliberately trade a small delay for smoother motion.
- **main.swift:** menu controls, settings coordination, snapshot preparation/reveal, overlay ordering and click blocking, sensor/capture coordination, and cleanup across sleep, display changes, cancellation, and quit.
- **SettingsWindow.swift:** native SwiftUI settings interface hosted in an AppKit window, including live sensor and permission state, persisted animation controls, and concise privacy information.

The first screenshot frame is rendered while the window is transparent. An 80 ms fade reveals the unchanged screenshot before the angle handoff begins. Returning to flat precedes an 80 ms exit fade. Snapshot tokens and renderer transition generations reject stale asynchronous work.

This is a frozen image viewed through a simulated moving panel. It does not track the viewer's head, intercept the lock screen, or keep the system awake.

The settings window opens directly on the experience page, with native controls and semantic system colors. The footer opens Privacy & About; a Back button (⌘[) returns to the experience. There is no shared logo header or segmented navigation. An original Canvas illustration projects synthetic screen contours and a MacBook lid in perspective; the illustration follows the live reading, but does not reproduce the Metal effect pixel for pixel. SettingsModel synchronizes trigger controls, enable state, live angle, permission, and runtime status. The settings view turns these into contextual readiness and first-gesture guidance. Privacy and alpha limitations live in About, also opened by the standard About Glissform menu command.

The in-window preview has local observable state and a finite SwiftUI task. It draws a synthetic close/reopen cycle, stops on request or navigation away, and never calls DesktopCapture or the overlay renderer. Reduce Motion replaces playback with a still preview and disables interpolation of the live illustration. All content surfaces are opaque and adapt to system appearance; controls use the system accent. These accessibility changes apply to the settings illustration, not to the actual desktop effect.

Settings buttons share a ButtonStyle for hover and pressed feedback; activation and cancellation remain owned by SwiftUI Button. The AppKit slider preserves system tracking and keyboard input with a solid-white thumb that changes size slightly on hover/press. The native stepper brightens in place on hover. A SwiftUI capsule switch keeps its thumb solid white, applies hover/press feedback to the track, matches the System Settings proportions, uses Button for activation, and exposes a native Toggle accessibility representation. None of these three controls adds an outer hover surface or border. The angle field adds an editing outline. The settings window ends field editing on an outside mouse click before forwarding that same event, so other controls still respond on the first click. Feedback overlays do not intercept input, disabled controls suppress feedback, and transitions respect Reduce Motion.
